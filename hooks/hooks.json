{
  "description": "git commit 시 intent 기록을 제안한다(제안만, 검수 생략 없음).",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          { "type": "command", "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/scripts/suggest-on-commit.sh", "timeout": 10 }
        ]
      }
    ]
  }
}
